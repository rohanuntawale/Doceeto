/**
 * Back-compat re-export. The catalog CONFIG now lives in lib/catalog.ts
 * (shared with the server, which prices orders from it). There is NO
 * demo data here or anywhere — everything is created via real flows.
 */
export { DARK_STORES, MED_CATALOG, AVATAR_COLORS } from "@/lib/catalog";
